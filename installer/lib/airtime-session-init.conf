instance WEB_USER

stop on runlevel [016]

script
  uid=WEB_USER
  HOME=WEB_ROOT

  export XDG_RUNTIME_DIR="/run/airtime"
  export HOME

  exec su -s /bin/sh -c 'exec "$0" "$@"' WEB_USER -- init --user
end script